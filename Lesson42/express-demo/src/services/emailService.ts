const sendEmail = () => {
  // This is a placeholder function that should take necessary input as parameters, then call an email service that you instantiate in the common folder (.e.g https://resend.com/) to send email. This file acts as an abstraction layer so that you can easily switch the library that actually sends the email without a need to update code in multiple places in the application.
}



export default {
  sendEmail
}